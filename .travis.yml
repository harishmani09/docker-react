language: generic
sudo: required


services:
    - docker


before_install:
    - docker build -t harishmani09/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true harishmani09/docker-react npm run test


deploy:
    provider: elasticbeanstalk
    region: ap-south-1
    app: "docker-react"
    env: "DockerReact-env"
    bucket_name: "elasticbeanstalk-ap-south-1-391160630794"
    bucket_path: "docker-react"
    on:
        branch: master
    
        

